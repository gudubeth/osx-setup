# OS X Node Setup

# Assumes: 
# + Homebrew has been installed correctly.

#npm will be installed with node
brew install node

npm install -g grunt-cli        #js task runner

npm install -g bower            #package manager for the web